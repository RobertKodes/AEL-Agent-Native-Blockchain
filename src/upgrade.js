import { AelEngine } from './engine.js';import{hash}from'./canonical.js';
export function rehearseUpgrade(previousState,migrate){try{const run=()=>{const migrated=migrate(structuredClone(previousState));const engine=new AelEngine(migrated);engine.assertInvariants();return engine.state},a=run(),b=run();if(hash(a)!==hash(b))return{allowed:false,reason:'NONDETERMINISTIC_MIGRATION'};if(a.schemaVersion===previousState.schemaVersion)return{allowed:false,reason:'VERSION_NOT_ADVANCED'};return{allowed:true,stateHash:hash(a),state:a};}catch(error){return{allowed:false,reason:error.code??error.message};}}
